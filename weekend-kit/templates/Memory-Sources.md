# Memory Sources — the lossless index
One row per raw memory file, what it holds, and which vault page covers it.
A vault page must be shorter and clearer than the memory files it draws from, or it should not exist.

| Memory file | What it holds (one line) | Vault page that covers it |
|---|---|---|
| `MEMORY.md` | the index itself | [[Home]] |
